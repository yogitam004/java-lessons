public class ArrayEx1 {

    public static void main(String args[]) {
        int[] price = {10,1,3,4,6,5,6,7,9,2};
        int[] number = new int[20];

        for(int i=0; i <number.length; i++) {
            number[i] = 4;
        }

        System.out.println(number);

        for(int i=0;i<price.length;i++0){
            if (price[i])
        }
    }

}
public class ArrayEx1 {

    public static void main(String args[]) {
        //Challenge 1: 
        int[] price = {10,1,3,4,6,5,6,7,9,2};

        //Challenge 2: 
        int[] number = new int[20];

        for(int i=0; i <number.length; i++) {
            number[i] = 4;
        }

        System.out.println(number);

        //Challenge 3:

        //Solution1: 

        double midpoint = (data.length)/2;
        double num = (price.length) % 2;
        double median = 0;
        if num == 0 {
            median = data[(int) midpoint];
        } else { num == 0 
            int midnum1 = data[(int)(midpoint-1)];
            int midnum2 = data[(int)(midpoint+1)];
            median = (midnum1 + midnum2)/2;
        }
        System.out.println("Median: " + median);

        //Solution 2: 

        int[] median = new int{13,4,12,8,9,3};
        int len = median.length;
        if (len % 2==1) {
            System.out.println(median[len/2]);
        } else {
            System.out.println(((median[(len/2)-1]) + (median[len/2]))/2);
        }
    }

}